// Zed keymap — https://zed.dev/docs/key-bindings
// Run `zed: open default keymap` from the command palette to see defaults.
[
  {
    "context": "Workspace",
    "bindings": {
      // "cmd-shift-e": "project_panel::ToggleFocus"
    }
  }
]
